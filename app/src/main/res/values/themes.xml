<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ClimAlert" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ClimAlert" parent="Base.Theme.ClimAlert" />

    <!--stile per la navBar -->
    <style name="CustomBottomNavigationView" parent="Widget.Material3.BottomNavigationView">
        <item name="materialThemeOverlay">@style/CustomBottomNavigationView.Overlay</item>
    </style>

    <!-- stile per il colore della selezione -->
    <style name="CustomBottomNavigationView.Overlay" parent="">
        <item name="colorSecondaryContainer">@color/white</item>
    </style>

</resources>